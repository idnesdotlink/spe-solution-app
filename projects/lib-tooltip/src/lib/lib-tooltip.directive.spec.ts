import { LibTooltipDirective } from './lib-tooltip.directive';

describe('LibTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new LibTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
