import { NgxRatingPipe } from './ngx-rating.pipe';

describe('NgxRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
