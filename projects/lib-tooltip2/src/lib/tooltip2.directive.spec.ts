import { Tooltip2Directive } from './tooltip2.directive';

describe('Tooltip2Directive', () => {
  it('should create an instance', () => {
    const directive = new Tooltip2Directive();
    expect(directive).toBeTruthy();
  });
});
