import { NgxColorPickerDirective } from './ngx-color-picker.directive';

describe('NgxColorPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxColorPickerDirective();
    expect(directive).toBeTruthy();
  });
});
