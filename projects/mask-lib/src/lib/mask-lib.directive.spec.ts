import { MaskLibDirective } from './mask-lib.directive';

describe('MaskLibDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskLibDirective();
    expect(directive).toBeTruthy();
  });
});
