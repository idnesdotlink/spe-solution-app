import { ApplierDirective } from './applier.directive';

describe('ApplierDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplierDirective();
    expect(directive).toBeTruthy();
  });
});
